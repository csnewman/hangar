# Local working notes — not part of the repo
AGENTS.md
CLAUDE.md
docs/
.claude/

# Build output
/out/
/hangar
/fs/target/
/gpu/target/

# Reference checkouts of other VMMs, for reading only.
# Underscore-prefixed so the Go toolchain does not walk into them.
/_refs/

# The built web UI, embedded by internal/webui.
/internal/webui/dist/app/
/web/node_modules/

# The packaged VS Code extension.
/vscode/*.vsix

# dev/reset-db.sh keeps its last copy of the data here.
/dev/.reset-db-dump.sql
